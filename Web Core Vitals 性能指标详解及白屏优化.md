# Web Core Vitals 性能指标详解 及白屏优化

```plaintxt
用户打开页面的完整时间线：

0ms        500ms       1s        1.5s       2s        2.5s       3s
│───────────│───────────│──────────│──────────│──────────│──────────│
│           │           │          │          │          │          │
├─ TTFB ──→│           │          │          │          │          │
│(首字节)   │           │          │          │          │          │
│           ├─ FCP ───→│          │          │          │          │
│           │(首次内容)  │          │          │          │          │
│           │           ├─ LCP ──→│          │          │          │
│           │           │(最大内容) │          │          │          │
│           │           │          ├─ FID ──→│          │          │
│           │           │          │(首次交互) │          │          │
│           │           │          │          ├─ TTI ──→│          │
│           │           │          │          │(可交互)   │          │
│           │           │          │          │          ├─ CLS    │
│           │           │          │          │          │(布局偏移) │

用户打开页面的完整生命周期：

0ms    请求发出
  ↓
TTFB   ──→ 收到第一个字节（服务端响应速度）
  ↓
FCP    ──→ 第一个内容绘制（用户看到第一个元素）
  ↓
LCP    ──→ 最大内容绘制（用户看到主要内容）
  ↓
TTI    ──→ 可交互时间（页面完全可操作）
  ↓
FID/INP ──→ 输入延迟（用户操作的响应速度）
  ↓
CLS    ──→ 布局偏移（页面元素是否乱跳）

```


## TTFB (Time to First Byte)
定义: 首字节时间，表示从浏览器发起请求到接收到服务器返回的第一个字节的时间。
详细解读: TTFB指标衡量了服务器响应速度。优化TTFB的关键在于减少服务器响应时间，确保浏览器能够尽快接收到服务器的响应数据。
```plaintext
用户点击链接
    ↓
DNS 解析 → TCP 连接 → SSL 握手 → 服务端处理 → 返回第一个字节
    ↓
全部加起来 = TTFB
```
***"说人话就是：服务器响应有多快"***
行业基准：

✅ 良好：≤ 800 毫秒
⚠️ 需要改进：800 - 1800 毫秒
❌ 较差：> 1800 毫秒


## FCP (First Contentful Paint)
定义：首次内容绘制，表示页面从开始加载到页面内容的任何部分在屏幕上首次渲染的时间。

详细解读：FCP是衡量用户感知页面加载速度的重要指标。它反映了页面内容的首次渲染时间，直接影响用户体验。优化FCP的关键在于减少关键资源的加载时间，确保页面内容能够 **尽快显示**。

***说人话就是：“用户看到第一个内容需要多久”***
行业基准：

✅ 良好：≤ 1.8 秒
⚠️ 需要改进：1.8 - 3.0 秒
❌ 较差：> 3.0 秒

## LCP  (Largest Contentful Paint)
定义：最大内容绘制，表示页面中最大内容元素在屏幕上渲染的时间。
详细解读: LCP指标衡量了页面中最大内容元素的渲染时间。优化LCP的关键在于减少最大内容元素的加载时间，确保页面主要内容能够尽快显示。

***说人话就是：“用户看到页面主要内容需要多久”***
行业基准：

✅ 良好：≤ 2.5 秒
⚠️ 需要改进：2.5 - 4.0 秒
❌ 较差：> 4.0 秒

## FID  (First Input Delay)
定义：首次输入延迟，表示用户首次与页面交互（如点击按钮）到页面实际响应的时间。
详细解读:  FID指标反映了页面的交互延迟。优化FID需要减少主线程的阻塞时间，确保页面能够快速响应用户的输入操作。

***说人话就是：“用户点击按钮后，页面多久能响应”***
行业基准：

✅ 良好：≤ 100 毫秒
⚠️ 需要改进：100 - 300 毫秒
❌ 较差：> 300 毫秒


## 全局视角：白屏的本质
```plaintxt
白屏就是用户从输入URL到看到页面内容之间的"空白等待时间"。这个过程涉及:

DNS解析 → TCP连接 → HTTP请求 → 下载HTML → 解析HTML → 下载CSS/JS → 执行JS → 渲染页面

每个环节都可能成为瓶颈，需要逐层击破


┌──────────────────────────────────────────────────────┐
│                  白屏优化全链路                         │
├──────────┬──────────┬──────────┬──────────┬──────────┤
│  网络层   │  构建层   │  资源层   │  渲染层   │  监控层  │
├──────────┼──────────┼──────────┼──────────┼──────────┤
│ CDN      │ 代码分割  │ 预加载   │ SSR/SSG  │ Lighthouse│
│ HTTP/2   │ Tree-shake│ 懒加载   │ 骨架屏   │ RUM      │
│ DNS预解析 │ 压缩     │ 缓存策略  │ 流式渲染  │ 告警     │
│ 预连接   │ 按需引入  │ Service  │ 渐进式   │ A/B测试  │
│          │          │ Worker   │ hydration│          │
└──────────┴──────────┴──────────┴──────────┴──────────┘
```